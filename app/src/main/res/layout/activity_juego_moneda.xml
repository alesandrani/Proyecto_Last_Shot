<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#D2F7EC">

    <!-- Botón Back -->
    <ImageButton
        android:id="@+id/btnBack"
        android:layout_width="60dp"
        android:layout_height="64dp"
        android:layout_marginTop="16dp"
        android:background="@android:color/transparent"
        android:contentDescription="Volver"
        android:src="@mipmap/button_back"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Botón Chat -->
    <ImageButton
        android:id="@+id/btnChat"
        android:layout_width="60dp"
        android:layout_height="64dp"
        android:layout_marginTop="16dp"
        android:background="@android:color/transparent"
        android:contentDescription="Chat"
        android:src="@mipmap/button_chat"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Barra Superior -->
    <LinearLayout
        android:id="@+id/barraSuperior"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:padding="16dp"
        app:layout_constraintTop_toBottomOf="@+id/btnBack"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">
        <!-- Aquí puedes añadir botones u otros elementos -->
    </LinearLayout>

    <!-- Lista de Jugadores -->
    <LinearLayout
        android:id="@+id/listaJugadores"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="30dp"
        app:layout_constraintTop_toBottomOf="@id/barraSuperior"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ImageButton
            android:id="@+id/btnJugador1"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:background="@drawable/pink"
            android:contentDescription="Jugador 1" />

        <ImageButton
            android:id="@+id/btnJugador2"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:layout_marginTop="16dp"
            android:background="@drawable/pink"
            android:contentDescription="Jugador 2" />

        <ImageButton
            android:id="@+id/btnJugador3"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:layout_marginTop="16dp"
            android:background="@drawable/pink"
            android:contentDescription="Jugador 3" />

        <ImageButton
            android:id="@+id/btnJugador4"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:layout_marginTop="16dp"
            android:background="@drawable/pink"
            android:contentDescription="Jugador 4" />

    </LinearLayout>

    <!-- Imagen del mechero -->

    <!-- Imagen de la moneda -->

    <!-- Sección de moneda -->

    <ImageView
        android:id="@+id/imgMonedaCara"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_marginTop="52dp"
        android:scaleType="fitCenter"
        android:src="@drawable/coin_cara"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.497"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/listaJugadores" />

    <LinearLayout
        android:id="@+id/monedaSection"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="horizontal"
        app:layout_constraintTop_toBottomOf="@id/imgMonedaCara"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp">
        <!-- Aquí puedes meter botones o textos de la moneda -->
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
